server:
  port: 8080
  error:
    include-message: always
    include-binding-errors : always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/tnote
    username : root
    password : rlaxoghks12

  devtools:
    livereload:
      enabled: false
    restart:
      enabled: false

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        validator:
          apply_to_ddl : false
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 1000 # 배치 사이즈 조절을 통해서 N+1이 나더라도 최소화 시킨다
    database: mysql

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1094168267661-n3b7sefr3t8i70j25hchguib6bntkmc5.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"
            authorization-grant-type: authorization_code
            scope: profile, email
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth?prompt=consent&access_type=offline
            token_uri: https://accounts.google.com/o/oauth2/token
            user-info-uri: https://www.googleapis.com/oauth2/v2/userinfo

custom:
  jwt:
    secret-key: tnotetnotetnotetnotetnotetnotetnotetnotetnotetnotetnotetnotetnote

jasypt:
  encryptor:
    bean: jasyptStringEncryptor

# hibernate logging
logging:
  level:
    p6spy: info
    org:
      springframework:
        security: DEBUG